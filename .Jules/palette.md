## 2025-05-14 - Accessible Interactive Grids & Localization
**Learning:** Dense interactive elements like attendance grids require specific context for screen readers and tooltips for visual users. When the application is localized, these accessibility labels must match the user's language for consistency.
**Action:** Always provide descriptive `aria-label` and `title` for grid-based controls, incorporating row/column context and localized state descriptions.
